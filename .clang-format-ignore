# Unity's generator writes these, and it is rewritten on every build whose cases changed. Formatting
# it by hand is undone immediately, and formatting it in CI makes every build that regenerates one
# report a dirty tree.
test/**/unity_runner.c

# Generated from the predicates by tools/dev_env/gen_ascii_masks.py and gen_anchor_profiles.py.
# clang-format and the generators disagree about the tables, and the generator wins - formatting
# these makes test/harness.py generated report a dirty tree on every run.
src/ascii_persona_bitorum/ascii_persona_bitorum.h
src/impensa_ancorae_acus/impensa_ancorae_acus_*.h
